name: Example push to ipfs
on: [push]
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      WEB3_STORAGE_TOKEN: ${{ secrets.WEB3_STORAGE_TOKEN }}
    steps:
      - uses: actions/checkout@master
      - run: echo "this worked"
